------------------------------------------------------------------------------------------------------------------------
this module implements the runtime-library's wrapper for an operating system's file system
------------------------------------------------------------------------------------------------------------------------
module runtime.operating_system.file_system


exports
========================================================================================================================

    Path, DirectoryPath, FilePath


contains
========================================================================================================================

    --------------------------------------------------------------------------------------------------------------------
    A Path that may refer to a directory, but not to a file
    --------------------------------------------------------------------------------------------------------------------
    type DirectoryPath:{Path,directory}

    --------------------------------------------------------------------------------------------------------------------
    A Path that may refer to a file, but not to a directory
    --------------------------------------------------------------------------------------------------------------------
    type FilePath:{Path,file}

    --------------------------------------------------------------------------------------------------------------------
    Definition for a Path, which may refer to a file or a directory
    --------------------------------------------------------------------------------------------------------------------
    compound value Path

            # bundle_directory.create_if_missing(readable=all, writable=owner)
            source_files_directory = bundle_directory.create_path_to_subdirectory("source_files")
            destination = source_files_directory.create_path_to_file(source.relative_filename)
            destination.directory_path_of().create_if_missing(readable=all, writable=owner)
            source.copy_file_to(destination)
