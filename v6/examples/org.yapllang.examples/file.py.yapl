--------------------------------------------------------------------------------------------------------------------
-- A file that is used for testing purposes
--------------------------------------------------------------------------------------------------------------------
module org.yapllang.examples.file:

    ----------------------------------------------------------------------------------------------------------------
    -- A class that is used for testing purposes
    ----------------------------------------------------------------------------------------------------------------
    class foo:

        public facet bar:

            --------------------------------------------------------------------------------------------------------
            -- The fubar method, for testing purposes
            --------------------------------------------------------------------------------------------------------
            method fubar:
                inputs:
                    forage_in_garden references garden -- the garden that the rabbit should forage in
                returns:
                    eaten is integer -- the number of carrots eaten
                emits:
                    rabbit_moved is boolean -- if the rabbit moved to another square
                errors:
                    rabbit_couldnt_move -- the rabbit just couldn't move :(
                code:
                    pass

        public interface baz:

            --------------------------------------------------------------------------------------------------------
            -- The foobar method, for testing purposes
            --------------------------------------------------------------------------------------------------------
            method foobar:
                returns:
                    eaten is integer -- the number of carrots eaten
                errors:
                    rabbit_couldnt_move -- the rabbit just couldn't move :(
                code:
                    eaten = this.fubar(garden)
                    if eaten == fubar.rabbit_couldnt_move:
                        error foobar.rabbit_couldnt_move
                    else:
                        return eaten = eaten

        public trait beh:

            --------------------------------------------------------------------------------------------------------
            -- The foobaz method, for testing purposes
            --------------------------------------------------------------------------------------------------------
            method foobaz:
                code:
                    pass
